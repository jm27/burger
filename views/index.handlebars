<div class="row jumbotron">
    <h1 class="col-12"> Eat da burger!!</h1>
</div>
<div class="row jumbotron">
    <div class="col-12"> 
<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Waiting!<br>
    <input type="radio" name="devoured" value="0">  Devoured!
  </div>

  <button type="submit">Add Burger</button>
</form>
</div>>
</div>
<div class="row jumbotron">
    <h1 class="col-6"> Waiting to be eaten!!</h1>
    <h1 class="col-6"> Devoured!!</h1>
</div>
<div class="row jumbotron">
    <div class="col-6">
        <ul>
            {{#each burgers}}
            {{#unless devoured}}
            {{> burgers/burger-block devoured=true}}
            {{/unless}}
            {{/each}}
        </ul>
    </div>>

    <div class="col-6">
               <ul>
            {{#each burgers}}
            {{#if devoured}}
            {{> burgers/burger-block devoured=false}}
            {{/if}}
            {{/each}}
        </ul>
    </div>
</div>